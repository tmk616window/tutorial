table:
  schema: public
  name: statuses
array_relationships:
  - name: todos
    using:
      foreign_key_constraint_on:
        column: status_id
        table:
          schema: public
          name: todos
