table:
  schema: public
  name: users
array_relationships:
  - name: todos
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          schema: public
          name: todos
